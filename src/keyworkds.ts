export enum Keyword {
    _class = 'class',
    _function = 'function',
    _import = 'import',
    _if = 'if',
    _while = 'while',
    _for = 'for',
    _switch = 'switch',
    _case = 'case',
    _else = 'else',
}
